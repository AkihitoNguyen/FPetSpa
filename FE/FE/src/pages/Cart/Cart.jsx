import React, { useContext } from 'react'
import './Cart.css'
const Cart = () => {

  const {cartItems,product_list, removeFromCart} = useContext(Store)

  return (
    <div>

    </div>
  )
}

export default Cart